#!/bin/sh
if command -v pwsh >/dev/null 2>&1; then
	pwsh -NoProfile -File tools/build-module-bundle.ps1 || exit 1
else
	powershell.exe -NoProfile -ExecutionPolicy Bypass -File tools/build-module-bundle.ps1 || exit 1
fi
git add dist/modules.luau
